# Metro Expansion Playbook — pointer

The **master playbook lives in the Chicago repo** (the reference
implementation), at
[`DistrictExplorer-CHI/docs/METRO_EXPANSION_PLAYBOOK.md`](https://github.com/ThursdaysFamous/DistrictExplorer-CHI/blob/main/docs/METRO_EXPANSION_PLAYBOOK.md).

This fork deliberately carries no copy: the two copies drifted once already —
this repo's gained the NYC port's lessons while Chicago's stayed at the
original text — and were reconciled into the master in July 2026. Section
references in this repo's comments and docs (e.g. "METRO_EXPANSION_PLAYBOOK
§6b") refer to the master; Part II and the Status thread log there ARE the
NYC worked example, so this fork's history is recorded there too.

Fold new lessons from any port back into the master, in the Chicago repo,
per `docs/ENGINE_SYNC.md` (port the diff, not the prompt).
