# Build Playbook (engine contract + build log) — pointer

The **master lives in the Chicago repo** (the reference implementation), at
[`DistrictExplorer-CHI/docs/BUILD_PLAYBOOK_1.md`](https://github.com/ThursdaysFamous/DistrictExplorer-CHI/blob/main/docs/BUILD_PLAYBOOK_1.md).

This fork deliberately carries no copy: the two copies drifted once already —
the file that used to live here was a stale verbatim Chicago snapshot, missing
the master's newest entries and containing not one line of NYC history. The
engine contract defined there is enforced here by the ENGINE fences in
`index.html` / `sw.js` (see `docs/ENGINE_SYNC.md`).

NYC's own build history is archived in the Chicago repo at
[`docs/archive/METRO_EXPANSION_NYC.md`](https://github.com/ThursdaysFamous/DistrictExplorer-CHI/blob/main/docs/archive/METRO_EXPANSION_NYC.md).
Fold new lessons back into the master, in the Chicago repo, per
`docs/ENGINE_SYNC.md` (port the diff, not the prompt).
